{
    #
    # 10base
    #
    $systemId = $subscription{'SystemId'} || $nethupdate{'SystemID'} || "";

    if ($systemId ne "") {
        $OUT = $subscription{'NsRelease'} || $nethupdate{'NsRelease'} || '';
    } else {
        $OUT = '';
    }
}